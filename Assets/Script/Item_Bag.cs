using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item_Bag : MonoBehaviour
{
    public int Have_Gun_ammo;
    // Start is called before the first frame update
    void Start()
    {
        Have_Gun_ammo = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
